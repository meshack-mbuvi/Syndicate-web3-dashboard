import React from "react";
import PropTypes from "prop-types";

import Layout from "src/components/layout";

/**
 * Manages feeds page
 */
const SyndicatePage = () => {
  return <Layout>Will manage syndicates</Layout>;
};

SyndicatePage.propTypes = {
  props: PropTypes.any,
};

export default SyndicatePage;
